package edu.union.fsm;

import org.junit.runner.RunWith;
import org.junit.runners.Suite;
import edu.union.adt.graph.tests.chiarulf.*;


@RunWith(Suite.class)
@Suite.SuiteClasses
({
    FSMStoreTests.class
})
public class FSMSuite
{ // no implementation needed; above annotations do the work.
}
